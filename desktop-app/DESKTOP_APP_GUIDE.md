# Руководство по использованию десктопного приложения

## Обзор

Десктопное приложение FTR Registration - это полнофункциональный клиент для управления регистрациями на мероприятия FTR. Приложение работает полностью автономно с локальной базой данных и синхронизируется с сервером при наличии интернета.

## Основные возможности

### 1. Автономная работа
- Все данные хранятся локально в SQLite базе данных
- Полная функциональность без интернета
- Быстрый доступ к данным

### 2. Автоматическая синхронизация
- Автоматическая синхронизация при наличии интернета
- Конфликты разрешаются автоматически (приоритет сервера)
- История изменений сохраняется

### 3. Кросс-платформенность
- Windows 10/11
- macOS 10.14+
- Linux (Ubuntu 20.04+, Debian 11+, и др.)

## Интерфейс

### Главное окно

После входа вы увидите главное окно с вкладками:

1. **События** - управление мероприятиями
2. **Регистрации** - управление регистрациями участников
3. **Оплаты** - управление финансовыми операциями
4. **Статистика** - просмотр статистики и отчетов

### Работа с событиями

- Просмотр списка всех событий
- Фильтрация по статусу
- Создание нового события (синхронизируется с сервером)
- Редактирование существующих событий

### Работа с регистрациями

- Просмотр всех регистраций для выбранного события
- Поиск по коллективу, названию номера, руководителю
- Создание новой регистрации
- Редактирование регистрации
- Управление участниками, дипломами, медалями

### Работа с оплатами

- Просмотр всех платежей
- Фильтрация по методу оплаты, статусу
- Создание ручных платежей
- Группировка платежей
- Экспорт в Excel/CSV

### Статистика

- Общая статистика по событию
- Статистика по номинациям
- Статистика по дисциплинам
- Статистика по оплатам

## Синхронизация

### Автоматическая синхронизация

По умолчанию приложение синхронизируется автоматически каждые 60 секунд (настраивается в `.env`).

### Ручная синхронизация

Нажмите кнопку "Синхронизировать" в верхней панели для немедленной синхронизации.

### Статус синхронизации

В правом нижнем углу отображается статус:
- 🟢 **Синхронизировано** - все данные актуальны
- 🟡 **Синхронизация...** - идет процесс синхронизации
- 🔴 **Оффлайн** - нет подключения к серверу
- ⚠️ **Конфликт** - требуется ручное разрешение конфликта

## Оффлайн режим

### Что работает оффлайн

- ✅ Просмотр всех данных
- ✅ Создание новых регистраций
- ✅ Редактирование существующих данных
- ✅ Просмотр статистики
- ✅ Экспорт данных

### Что требует интернет

- ❌ Первоначальная загрузка данных
- ❌ Синхронизация изменений с сервером
- ❌ Авторизация (токен кэшируется)

## Разрешение конфликтов

Если при синхронизации обнаружен конфликт (одна и та же запись изменена и локально, и на сервере):

1. Приоритет отдается данным с сервера
2. Локальные изменения сохраняются в истории
3. Вы можете просмотреть историю изменений и восстановить локальные изменения при необходимости

## Экспорт данных

### Экспорт в Excel

1. Выберите нужные данные (регистрации, оплаты и т.д.)
2. Нажмите "Экспорт" → "Excel"
3. Выберите место сохранения файла

### Экспорт в CSV

Аналогично экспорту в Excel, выберите формат CSV.

## Настройки

### Настройка интервала синхронизации

Отредактируйте файл `.env`:

```env
SYNC_INTERVAL=120  # секунды между синхронизациями
```

### Отключение автоматической синхронизации

```env
AUTO_SYNC=false
```

### Изменение уровня логирования

```env
LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR
```

## Производительность

### Оптимизация для больших объемов данных

1. Используйте фильтры для уменьшения количества отображаемых записей
2. Регулярно синхронизируйтесь для обновления индексов
3. При необходимости очистите локальную БД и выполните полную синхронизацию

### Размер базы данных

Локальная база данных обычно занимает:
- Небольшие события (< 100 регистраций): ~1-5 MB
- Средние события (100-1000 регистраций): ~5-20 MB
- Большие события (> 1000 регистраций): ~20-100 MB

## Безопасность

### Хранение учетных данных

- Токен авторизации хранится в памяти приложения
- Пароль никогда не сохраняется локально
- При закрытии приложения токен удаляется

### Локальная база данных

- База данных не зашифрована по умолчанию
- Для повышения безопасности можно использовать SQLCipher (требует дополнительной настройки)

## Советы и рекомендации

1. **Регулярная синхронизация**: Синхронизируйтесь регулярно для актуальности данных
2. **Резервное копирование**: Периодически делайте копию локальной БД
3. **Фильтры**: Используйте фильтры для быстрого поиска нужных данных
4. **Экспорт**: Регулярно экспортируйте важные данные для резервного копирования

## Поддержка

При возникновении проблем:
1. Проверьте логи в `~/.local/share/ftr_registration/logs/app.log`
2. Создайте issue в репозитории проекта с описанием проблемы
3. Приложите соответствующие логи (уберите чувствительную информацию)

