# 🚀 Быстрый запуск проекта в Docker

Пошаговая инструкция по запуску FTR Registration System в Docker.

## 📋 Шаг 1: Подготовка

### 1.1. Клонирование репозитория

```bash
git clone https://github.com/punk03/FTR_REG.git
cd FTR_REG
```

### 1.2. Создание файла переменных окружения

```bash
cp .env.example .env
```

### 1.3. Настройка переменных окружения

Откройте файл `.env` и обязательно измените следующие значения:

```env
# ОБЯЗАТЕЛЬНО измените эти значения для безопасности!
JWT_SECRET=ваш-случайный-секретный-ключ-минимум-32-символа
JWT_REFRESH_SECRET=ваш-другой-случайный-секретный-ключ-минимум-32-символа

# Пароль для базы данных (измените на надёжный)
POSTGRES_PASSWORD=ваш-надёжный-пароль-для-бд

# Если нужно изменить порты (по умолчанию используются стандартные)
BACKEND_PORT=3001
FRONTEND_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379
```

**Важно**: Для генерации случайных секретов можно использовать:
```bash
# Linux/Mac
openssl rand -base64 32

# Или онлайн генератор: https://www.random.org/strings/
```

## 🐳 Шаг 2: Запуск проекта

### 2.1. Запуск всех сервисов

```bash
docker-compose up -d --build
```

Эта команда:
- Соберёт образы для backend и frontend
- Создаст и запустит контейнеры (PostgreSQL, Redis, Backend, Frontend)
- Применит миграции базы данных автоматически

### 2.2. Проверка статуса

```bash
docker-compose ps
```

Вы должны увидеть все 4 сервиса в статусе `Up`:
- `ftr_postgres` - база данных
- `ftr_redis` - кэш
- `ftr_backend` - API сервер
- `ftr_frontend` - веб-интерфейс

### 2.3. Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f postgres
```

## ✅ Шаг 3: Проверка работоспособности

### 3.1. Проверка Backend

```bash
# Проверка health endpoint
curl http://localhost:3001/api/health

# Должен вернуть JSON с информацией о статусе сервисов
```

### 3.2. Проверка Frontend

Откройте в браузере: **http://localhost:3000**

Вы должны увидеть страницу входа в систему.

### 3.3. Заполнение базы данных тестовыми данными (опционально)

```bash
docker-compose exec backend npm run prisma:seed
```

Это создаст:
- Тестовых пользователей (ADMIN, REGISTRATOR, ACCOUNTANT, STATISTICIAN)
- Справочники (дисциплины, номинации, возрасты, категории)
- Тестовое мероприятие

**Учётные данные по умолчанию:**
- **ADMIN**: admin@ftr.ru / admin123
- **REGISTRATOR**: registrator@ftr.ru / registrator123
- **ACCOUNTANT**: accountant@ftr.ru / accountant123
- **STATISTICIAN**: statistician@ftr.ru / statistician123

## 🔧 Шаг 4: Управление проектом

### Остановка проекта

```bash
docker-compose down
```

### Перезапуск проекта

```bash
docker-compose restart
# или
docker-compose down && docker-compose up -d
```

### Пересборка после изменений кода

```bash
# Пересборка всех сервисов
docker-compose up -d --build

# Пересборка только backend
docker-compose up -d --build backend

# Пересборка только frontend
docker-compose up -d --build frontend
```

### Просмотр использования ресурсов

```bash
docker stats
```

## 🗄️ Шаг 5: Работа с базой данных

### Подключение к PostgreSQL

```bash
docker-compose exec postgres psql -U ftr_user -d ftr_db
```

### Резервное копирование БД

```bash
# Создать backup
docker-compose exec postgres pg_dump -U ftr_user ftr_db > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из backup
docker-compose exec -T postgres psql -U ftr_user -d ftr_db < backup_20231207_120000.sql
```

### Применение миграций вручную

```bash
docker-compose exec backend npx prisma migrate deploy
```

### Открыть Prisma Studio (GUI для БД)

```bash
docker-compose exec backend npx prisma studio
```

Откроется веб-интерфейс на http://localhost:5555

## 🐛 Решение проблем

### Проблема: Контейнеры не запускаются

```bash
# Проверьте логи
docker-compose logs

# Проверьте, не заняты ли порты
# Linux/Mac
lsof -i :3000 -i :3001 -i :5432 -i :6379

# Windows
netstat -ano | findstr "3000 3001 5432 6379"
```

### Проблема: Backend не подключается к БД

```bash
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Проверьте логи PostgreSQL
docker-compose logs postgres

# Проверьте переменную DATABASE_URL в .env
# Должна быть: postgresql://ftr_user:ftr_password@postgres:5432/ftr_db?schema=public
```

### Проблема: Frontend не открывается

```bash
# Проверьте логи frontend
docker-compose logs frontend

# Проверьте, что backend запущен
curl http://localhost:3001/api/health

# Пересоберите frontend
docker-compose up -d --build frontend
```

### Проблема: Порты заняты

**Ошибка:** `failed to bind host port 0.0.0.0:3001/tcp: address already in use`

Это означает, что порт 3001 уже занят другим процессом. Есть два решения:

#### Решение 1: Найти и остановить процесс на порту 3001

```bash
# Найти процесс, использующий порт 3001
sudo lsof -i :3001
# или
sudo ss -tlnp | grep :3001
# или
sudo netstat -tlnp | grep :3001

# Остановить процесс (замените PID на реальный ID процесса)
sudo kill -9 <PID>

# Или если это старый backend процесс
pkill -f "node.*backend"
# или
pkill -f "npm.*start"

# Затем снова запустите Docker
docker-compose up -d
```

#### Решение 2: Изменить порт в `.env`

Если вы не можете остановить процесс на порту 3001, измените порт в `.env`:

```env
BACKEND_PORT=3002
```

И обновите `VITE_API_URL` в `.env`:
```env
VITE_API_URL=http://95.71.125.8:3002
```

Затем пересоберите и перезапустите:
```bash
docker-compose down
docker-compose up -d --build
```

**Примечание:** Если вы изменили порт backend, не забудьте также обновить `CORS_ORIGIN` в `.env`, чтобы включить новый порт frontend (если он тоже изменился).

### Полная очистка и перезапуск

```bash
# Остановить и удалить контейнеры
docker-compose down

# Удалить volumes (ОСТОРОЖНО: удалит все данные БД!)
docker-compose down -v

# Пересобрать и запустить заново
docker-compose up -d --build
```

## 📊 Мониторинг

### Просмотр логов в реальном времени

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Последние 100 строк
docker-compose logs --tail=100 backend
```

### Проверка здоровья сервисов

```bash
# Backend health
curl http://localhost:3001/api/health

# Database health
curl http://localhost:3001/api/health/db

# Redis health
curl http://localhost:3001/api/health/redis
```

## 🔐 Безопасность для Production

Перед развёртыванием в production:

1. **Измените все секреты** в `.env`
2. **Используйте сильные пароли** для БД
3. **Настройте CORS** правильно:
   ```env
   CORS_ORIGIN=https://yourdomain.com
   ```
4. **Используйте HTTPS** через reverse proxy
5. **Не публикуйте порты БД и Redis** наружу (уберите `ports` из docker-compose.yml для postgres и redis)

## 📚 Дополнительная информация

- Подробная документация: `DOCKER_SETUP.md`
- Структура проекта: `README.md`
- Инструкция по установке: `INSTALLATION_GUIDE.md`

## ✅ Чеклист запуска

- [ ] Репозиторий склонирован
- [ ] Файл `.env` создан из `.env.example`
- [ ] Секреты (JWT_SECRET, JWT_REFRESH_SECRET) изменены
- [ ] Пароль БД изменён
- [ ] `docker-compose up -d --build` выполнен успешно
- [ ] Все контейнеры в статусе `Up` (`docker-compose ps`)
- [ ] Backend отвечает (`curl http://localhost:3001/api/health`)
- [ ] Frontend открывается (http://localhost:3000)
- [ ] База данных заполнена тестовыми данными (опционально)

Готово! Проект должен работать. 🎉

